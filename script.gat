.gat
<NodeJSExtension.9(SIX)mode>
this is the content for NODE.JS
<p>(user) = variable
  user = password (tempgat0221)
  user = obn (verfice)

  obn = [panface]

  user = fab.api (password) = password

  obn = [ID] web (userinterface)

  obn = INSTALL (githubclient.gambl) = (client)

  client.run (offpendrive) = (MINT)
  MINT = dec set (yoohuian) client.run
offpendrive = (set) client (art) variable. (edit)
  edit = set (client) art (password)
